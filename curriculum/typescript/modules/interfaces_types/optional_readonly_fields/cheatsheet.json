{
  "title": "Optional Readonly Fields Cheat Sheet",
  "printNote": "Quick reference for Optional Readonly Fields in TypeScript.",
  "sections": [
    {
      "heading": "Syntax & Usage",
      "entries": [
        {
          "syntax": "// TypeScript implementation for Optional Readonly Fields",
          "example": "// TypeScript implementation for Optional Readonly Fields\ninterface OptionalReadonlyFieldsPayload {\n  id: number;\n  title: string;\n  status: 'active' | 'pending';\n}\n\nfunction processOptionalReadonlyFields(item: OptionalReadonlyFieldsPayload): string {\n  return `[${item.id}] ${item.title}: ${item.status}`;\n}\n\nconsole.log(processOptionalReadonlyFields({ id: 1, title: 'Optional Readonly Fields', status: 'active' }));",
          "description": "Basic syntax for Optional Readonly Fields",
          "commonMistake": "Syntax error or missing colon/semicolon"
        }
      ]
    }
  ]
}