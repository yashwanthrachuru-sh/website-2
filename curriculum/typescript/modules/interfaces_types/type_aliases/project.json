{
  "title": "Type Aliases Project",
  "tagline": "Build a mini application using Type Aliases",
  "description": "Create a working TypeScript application demonstrating Type Aliases.",
  "requirements": [
    "Follow standard style guidelines",
    "Handle errors gracefully",
    "Produce clear, verifiable output"
  ],
  "starterCode": "// TypeScript implementation for Type Aliases\ninterface TypeAliasesPayload {\n  id: number;\n  title: string;\n  status: 'active' | 'pending';\n}\n\nfunction processTypeAliases(item: TypeAliasesPayload): string {\n  return `[${item.id}] ${item.title}: ${item.status}`;\n}\n\nconsole.log(processTypeAliases({ id: 1, title: 'Type Aliases', status: 'active' }));",
  "solution": "// TypeScript implementation for Type Aliases\ninterface TypeAliasesPayload {\n  id: number;\n  title: string;\n  status: 'active' | 'pending';\n}\n\nfunction processTypeAliases(item: TypeAliasesPayload): string {\n  return `[${item.id}] ${item.title}: ${item.status}`;\n}\n\nconsole.log(processTypeAliases({ id: 1, title: 'Type Aliases', status: 'active' }));",
  "expectedOutput": "Project Completed Successfully",
  "solutionExpl": "This solution demonstrates clean application of Type Aliases in TypeScript."
}