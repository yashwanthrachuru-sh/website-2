{
  "title": "Partial and Required Project",
  "tagline": "Build a mini application using Partial and Required",
  "description": "Create a working TypeScript application demonstrating Partial and Required.",
  "requirements": [
    "Follow standard style guidelines",
    "Handle errors gracefully",
    "Produce clear, verifiable output"
  ],
  "starterCode": "// TypeScript implementation for Partial and Required\ninterface PartialandRequiredPayload {\n  id: number;\n  title: string;\n  status: 'active' | 'pending';\n}\n\nfunction processPartialandRequired(item: PartialandRequiredPayload): string {\n  return `[${item.id}] ${item.title}: ${item.status}`;\n}\n\nconsole.log(processPartialandRequired({ id: 1, title: 'Partial and Required', status: 'active' }));",
  "solution": "// TypeScript implementation for Partial and Required\ninterface PartialandRequiredPayload {\n  id: number;\n  title: string;\n  status: 'active' | 'pending';\n}\n\nfunction processPartialandRequired(item: PartialandRequiredPayload): string {\n  return `[${item.id}] ${item.title}: ${item.status}`;\n}\n\nconsole.log(processPartialandRequired({ id: 1, title: 'Partial and Required', status: 'active' }));",
  "expectedOutput": "Project Completed Successfully",
  "solutionExpl": "This solution demonstrates clean application of Partial and Required in TypeScript."
}