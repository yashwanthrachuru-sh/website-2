{
  "title": "TS Class Fields Cheat Sheet",
  "printNote": "Quick reference for TS Class Fields in TypeScript.",
  "sections": [
    {
      "heading": "Syntax & Usage",
      "entries": [
        {
          "syntax": "// TypeScript implementation for TS Class Fields",
          "example": "// TypeScript implementation for TS Class Fields\ninterface TSClassFieldsPayload {\n  id: number;\n  title: string;\n  status: 'active' | 'pending';\n}\n\nfunction processTSClassFields(item: TSClassFieldsPayload): string {\n  return `[${item.id}] ${item.title}: ${item.status}`;\n}\n\nconsole.log(processTSClassFields({ id: 1, title: 'TS Class Fields', status: 'active' }));",
          "description": "Basic syntax for TS Class Fields",
          "commonMistake": "Syntax error or missing colon/semicolon"
        }
      ]
    }
  ]
}