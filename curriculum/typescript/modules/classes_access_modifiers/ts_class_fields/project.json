{
  "title": "TS Class Fields Project",
  "tagline": "Build a mini application using TS Class Fields",
  "description": "Create a working TypeScript application demonstrating TS Class Fields.",
  "requirements": [
    "Follow standard style guidelines",
    "Handle errors gracefully",
    "Produce clear, verifiable output"
  ],
  "starterCode": "// TypeScript implementation for TS Class Fields\ninterface TSClassFieldsPayload {\n  id: number;\n  title: string;\n  status: 'active' | 'pending';\n}\n\nfunction processTSClassFields(item: TSClassFieldsPayload): string {\n  return `[${item.id}] ${item.title}: ${item.status}`;\n}\n\nconsole.log(processTSClassFields({ id: 1, title: 'TS Class Fields', status: 'active' }));",
  "solution": "// TypeScript implementation for TS Class Fields\ninterface TSClassFieldsPayload {\n  id: number;\n  title: string;\n  status: 'active' | 'pending';\n}\n\nfunction processTSClassFields(item: TSClassFieldsPayload): string {\n  return `[${item.id}] ${item.title}: ${item.status}`;\n}\n\nconsole.log(processTSClassFields({ id: 1, title: 'TS Class Fields', status: 'active' }));",
  "expectedOutput": "Project Completed Successfully",
  "solutionExpl": "This solution demonstrates clean application of TS Class Fields in TypeScript."
}