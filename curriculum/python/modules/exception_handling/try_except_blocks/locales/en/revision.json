{
  "summary": "`try` wraps code, `except` catches errors, `else` runs if no error, `finally` always runs.",
  "oneLineSummary": "try-except handles runtime exceptions gracefully.",
  "keyTakeaways": [
    "Catch specific exceptions.",
    "EAFP is pythonic."
  ],
  "memoryTricks": [
    {
      "concept": "try-except",
      "trick": "TRY it, EXCEPT if it breaks, ELSE if it worked, FINALLY clean up."
    }
  ],
  "commonErrors": [
    {
      "error": "Bare except",
      "cause": "Catches KeyboardInterrupt",
      "fix": "Use except Exception"
    }
  ],
  "preInterviewChecklist": [
    "Explain EAFP vs LBYL"
  ],
  "nextTopics": [
    {
      "title": "Raising Exceptions",
      "whyNext": "Learn to raise custom error triggers."
    }
  ]
}