{
  "title": "Bank Account Rule Guard",
  "tagline": "Business logic rule enforcer",
  "description": "Build a bank account class raising ValueError on negative deposits and OverdraftError on excess withdrawals.",
  "learningGoals": [
    "Enforcing invariants with raise"
  ],
  "requirements": [
    "Raise ValueError if deposit <= 0"
  ],
  "starterCode": "class Account:\n    def __init__(self, bal): self.bal = bal",
  "solution": "class Account:\n    def __init__(self, bal=0):\n        self.bal = bal\n    def deposit(self, amt):\n        if amt <= 0: raise ValueError('Deposit must be positive')\n        self.bal += amt",
  "solutionExpl": "Raises ValueError when deposit constraint is violated.",
  "expectedOutput": "100",
  "extensions": [
    "Add overdraft limit check"
  ]
}