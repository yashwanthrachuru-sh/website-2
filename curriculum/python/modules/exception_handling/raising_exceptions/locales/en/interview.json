{
  "questions": [
    {
      "question": "What is exception chaining in Python 3?",
      "answer": "Exception chaining using `raise NewError() from original_error` attaches the original exception as `__cause__` to the new exception, printing both tracebacks so developers can trace root causes.",
      "level": "intermediate"
    }
  ]
}