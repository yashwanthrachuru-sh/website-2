{
  "deeperExplanation": "Custom exceptions can carry extra attributes (e.g., error codes, payload state, timestamps) by overriding `__init__()`.",
  "internalImplementation": "Custom exceptions inherit standard tuple `args` behavior from `BaseException`.",
  "examples": [
    {
      "code": "class ValidationError(Exception):\n    def __init__(self, message, field):\n        super().__init__(message)\n        self.field = field\n\ntry:\n    raise ValidationError('Email invalid', field='email')\nexcept ValidationError as e:\n    print(f'Field error on {e.field}: {e}')",
      "explanation": "Attaches `field` attribute to exception instance."
    }
  ],
  "performanceConsiderations": {
    "timeComplexity": "O(1) class instantiation.",
    "spaceComplexity": "O(1)"
  },
  "debuggingWalkthrough": {
    "bugDescription": "Inheriting from BaseException by mistake.",
    "incorrectCode": "class MyError(BaseException): pass",
    "correctCode": "class MyError(Exception): pass"
  },
  "bestPractices": [
    "Inherit from `Exception` (not `BaseException`).",
    "Define a base `AppException` for your package."
  ]
}