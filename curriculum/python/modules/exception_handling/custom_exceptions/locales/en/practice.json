{
  "easy": {
    "title": "Create NegativeValueError",
    "description": "Define `NegativeValueError(Exception)` and raise it if val < 0.",
    "starterCode": "# Define exception and test function",
    "expectedOutput": "Caught",
    "solution": "class NegativeValueError(Exception): pass\ndef check(v):\n    if v < 0: raise NegativeValueError('Negative!')",
    "hints": [
      "class NegativeValueError(Exception): pass"
    ],
    "difficulty": "easy"
  },
  "medium": {
    "title": "Custom Error with Error Code",
    "description": "Create `APIError(Exception)` storing `code` and `message`.",
    "starterCode": "class APIError(Exception):\n    pass",
    "expectedOutput": "404",
    "solution": "class APIError(Exception):\n    def __init__(self, msg, code):\n        super().__init__(msg)\n        self.code = code",
    "hints": [
      "Call super().__init__(msg)"
    ],
    "difficulty": "medium"
  },
  "hard": {
    "title": "Application Exception Hierarchy",
    "description": "Create `AppError` base, `AuthError` and `PermissionError` children.",
    "starterCode": "# Define hierarchy",
    "expectedOutput": "True",
    "solution": "class AppError(Exception): pass\nclass AuthError(AppError): pass\nclass PermissionError(AppError): pass",
    "hints": [
      "Inherit children from AppError"
    ],
    "difficulty": "hard"
  },
  "debugging": {
    "title": "Fix BaseException Inheritance",
    "description": "Fix inheritance from BaseException.",
    "buggyCode": "class CustomErr(BaseException): pass",
    "fixedCode": "class CustomErr(Exception): pass",
    "bugs": [
      "Inherit from `Exception`, not `BaseException`."
    ]
  }
}