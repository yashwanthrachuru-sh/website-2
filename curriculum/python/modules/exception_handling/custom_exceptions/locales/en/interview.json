{
  "questions": [
    {
      "question": "Why should custom exceptions inherit from `Exception` instead of `BaseException`?",
      "answer": "`BaseException` is the top-level base for `SystemExit`, `KeyboardInterrupt`, and `GeneratorExit`. Inheriting from `BaseException` would cause generic `except Exception:` blocks to miss your custom error and prevent Ctrl+C interrupt signals from stopping the program.",
      "level": "intermediate"
    }
  ]
}