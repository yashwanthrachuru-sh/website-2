{
  "examples": [
    {
      "code": "# Higher-Order Function returning a function\ndef make_multiplier(factor):\n    def multiply(number):\n        return number * factor\n    return multiply\n\ndouble = make_multiplier(2)\nprint(double(5))  # 10",
      "explanation": "`make_multiplier` creates a closure capturing `factor` in its environment."
    }
  ]
}