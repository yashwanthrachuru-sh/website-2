{
  "examples": [
    {
      "code": "import os\n# Atomic file write pattern via temporary file swap\ntmp_path = 'file.txt.tmp'\nwith open(tmp_path, 'w') as f:\n    f.write('Critical Data')\nos.replace(tmp_path, 'file.txt')  # Atomic OS swap!",
      "explanation": "Prevents corrupted partial file writes on crash."
    }
  ]
}