{
  "questions": [
    {
      "question": "How do you achieve atomic file writes in Python?",
      "answer": "Write data to a temporary file in the same directory, flush/sync it, then use `os.replace(tmp, target)`. `os.replace` is an atomic filesystem operation on POSIX and modern Windows.",
      "level": "intermediate"
    }
  ]
}