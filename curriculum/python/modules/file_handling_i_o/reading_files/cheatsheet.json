{
  "title": "Reading Files Cheat Sheet",
  "printNote": "Quick reference for Reading Files in Python.",
  "sections": [
    {
      "heading": "Syntax & Usage",
      "entries": [
        {
          "syntax": "# Python implementation for Reading Files",
          "example": "# Python implementation for Reading Files\ndef process_data(records):\n    \"\"\"Process input dataset cleanly.\"\"\"\n    results = [item.strip().upper() for item in records if item]\n    return results\n\nif __name__ == '__main__':\n    data = ['apple', 'banana', 'cherry']\n    print('Processed:', process_data(data))",
          "description": "Basic syntax for Reading Files",
          "commonMistake": "Syntax error or missing colon/semicolon"
        }
      ]
    }
  ]
}