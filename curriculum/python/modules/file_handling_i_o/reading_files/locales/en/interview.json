{
  "questions": [
    {
      "question": "Why is `with open(...)` preferred over calling `f.close()` manually?",
      "answer": "`with` uses the Context Manager protocol. It guarantees that `f.close()` is executed automatically when exiting the block, even if exceptions or early `return` statements occur.",
      "level": "beginner"
    }
  ]
}