{
  "examples": [
    {
      "code": "import mmap\n# Memory-mapped file for ultra-fast reading\nwith open('huge.bin', 'r+b') as f:\n    mm = mmap.mmap(f.fileno(), 0)\n    print(mm[:10])\n    mm.close()",
      "explanation": "mmap maps file bytes directly into process virtual memory address space."
    }
  ]
}