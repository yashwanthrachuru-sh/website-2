{
  "examples": [
    {
      "code": "from contextlib import ExitStack\n# Dynamic multi-resource management with ExitStack\nfiles = ['f1.txt', 'f2.txt']\nwith ExitStack() as stack:\n    handles = [stack.enter_context(open(fn, 'w')) for fn in files]\n    # All handles safely closed upon exit",
      "explanation": "ExitStack manages dynamic variable numbers of context resources."
    }
  ]
}