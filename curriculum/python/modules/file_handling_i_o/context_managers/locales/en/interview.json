{
  "questions": [
    {
      "question": "How does `@contextmanager` work internally in Python?",
      "answer": "`@contextmanager` converts a generator function into a context manager. On `__enter__`, it calls `next(gen)`, executing up to `yield`. On `__exit__`, it resumes the generator using `next(gen)` or `gen.throw(exception)` inside `finally`.",
      "level": "advanced"
    }
  ]
}