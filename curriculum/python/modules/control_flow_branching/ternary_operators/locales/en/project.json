{
  "title": "Dynamic Discount Pricing Engine",
  "tagline": "Inline conditional pricing rules",
  "description": "Calculate product price applying instant tier discounts with ternary logic.",
  "learningGoals": [
    "Compact inline condition evaluation"
  ],
  "requirements": [
    "Apply 20% discount if quantity >= 10, else 0%"
  ],
  "starterCode": "def calc_price(unit_price, qty):\n    pass",
  "solution": "def calc_price(unit_price, qty):\n    discount = 0.20 if qty >= 10 else 0.0\n    total = (unit_price * qty) * (1 - discount)\n    return round(total, 2)",
  "solutionExpl": "Uses single-line ternary to compute discount factor.",
  "expectedOutput": "160.0",
  "extensions": [
    "Format as currency string"
  ]
}