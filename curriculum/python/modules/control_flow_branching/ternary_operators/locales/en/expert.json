{
  "examples": [
    {
      "code": "# Ternary with Tuples/Dicts (Alternative evaluation pattern)\nstatus = ('Minor', 'Adult')[age >= 18]",
      "explanation": "Tuple indexing `(false_val, true_val)[cond]` works but loses short-circuit evaluation."
    }
  ]
}