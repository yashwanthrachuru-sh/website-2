{
  "title": "System Diagnostic Reporter",
  "tagline": "Stdlib system status tool",
  "description": "Build a system diagnostic tool using `os`, `sys`, `platform`, and `datetime` to generate an environment report.",
  "learningGoals": [
    "Utilizing stdlib modules together"
  ],
  "requirements": [
    "Report Python version, OS platform, timestamp"
  ],
  "starterCode": "def generate_report(): pass",
  "solution": "import sys, platform, datetime\ndef generate_report():\n    return {\n        'python_version': sys.version.split()[0],\n        'platform': platform.system(),\n        'timestamp': datetime.datetime.now().isoformat()\n    }",
  "solutionExpl": "Combines sys, platform, and datetime to build environment report.",
  "expectedOutput": "{'python_version': '3.12.0', ...}",
  "extensions": [
    "Add CPU architecture info"
  ]
}