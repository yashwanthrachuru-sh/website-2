{
  "examples": [
    {
      "code": "import collections, functools\n# lru_cache for memoization\n@functools.lru_cache(maxsize=128)\ndef fib(n):\n    if n < 2: return n\n    return fib(n-1) + fib(n-2)\nprint(fib(50))",
      "explanation": "functools.lru_cache provides instant memoization."
    }
  ]
}