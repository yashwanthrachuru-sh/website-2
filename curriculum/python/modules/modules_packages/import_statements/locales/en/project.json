{
  "title": "Plugin Dispatch Loader",
  "tagline": "Dynamic plugin architecture",
  "description": "Build a plugin loader dynamically importing modules from a plugins directory.",
  "learningGoals": [
    "Dynamic module importing"
  ],
  "requirements": [
    "Import plugin by name and execute run()"
  ],
  "starterCode": "def run_plugin(plugin_name): pass",
  "solution": "import importlib\ndef run_plugin(plugin_name):\n    mod = importlib.import_module(f'plugins.{plugin_name}')\n    return mod.run()",
  "solutionExpl": "Dynamically imports and calls run() on plugin module.",
  "expectedOutput": "'Plugin Executed'",
  "extensions": [
    "Add plugin validation check"
  ]
}