{
  "title": "Multi-Provider Payment Gateway",
  "tagline": "Polymorphic payment engine",
  "description": "Build unified payment processor handling Stripe, PayPal, and Square via polymorphic interface.",
  "learningGoals": [
    "Polymorphic design pattern"
  ],
  "requirements": [
    "Unified charge(amount) method"
  ],
  "starterCode": "# Payment Gateway setup",
  "solution": "class Stripe: \n    def charge(self, amt): return f'Stripe: ${amt}'\nclass PayPal:\n    def charge(self, amt): return f'PayPal: ${amt}'\n\ndef process(provider, amt):\n    return provider.charge(amt)",
  "solutionExpl": "Delegates charge execution polymorphically.",
  "expectedOutput": "'Stripe: $50'",
  "extensions": [
    "Add transaction logging"
  ]
}