{
  "examples": [
    {
      "code": "from typing import Protocol\n# Static Duck Typing with Protocol (PEP 544)\nclass Renderable(Protocol):\n    def render(self) -> str: ...\n\ndef display(item: Renderable):\n    print(item.render())",
      "explanation": "Protocol provides structural typing for static analyzers like mypy."
    }
  ]
}