{
  "title": "Student Gradebook Manager Class",
  "tagline": "OOP Student entity modeler",
  "description": "Build `Student` class tracking grades list and calculating GPA.",
  "learningGoals": [
    "Encapsulation of student data and GPA methods"
  ],
  "requirements": [
    "add_grade()",
    "get_gpa()"
  ],
  "starterCode": "class Student: pass",
  "solution": "class Student:\n    def __init__(self, name):\n        self.name = name\n        self.grades = []\n    def add_grade(self, g):\n        self.grades.append(g)\n    def get_gpa(self):\n        return sum(self.grades)/len(self.grades) if self.grades else 0.0",
  "solutionExpl": "Encapsulates grade list management and GPA calculations.",
  "expectedOutput": "85.0",
  "extensions": [
    "Add pass/fail status property"
  ]
}