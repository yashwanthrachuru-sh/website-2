{
  "examples": [
    {
      "code": "class LightweightPoint:\n    __slots__ = ['x', 'y'] # Eliminates __dict__ overhead!\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y",
      "explanation": "__slots__ saves ~60% RAM for millions of point instances."
    }
  ]
}