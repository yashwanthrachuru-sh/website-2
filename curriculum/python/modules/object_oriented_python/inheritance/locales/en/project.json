{
  "title": "RPG Character Class Hierarchy",
  "tagline": "Game entity inheritance system",
  "description": "Build base `Character` class and specialized `Warrior` / `Mage` classes.",
  "learningGoals": [
    "Inheritance and method overriding"
  ],
  "requirements": [
    "Warrior uses stamina, Mage uses mana"
  ],
  "starterCode": "class Character: pass",
  "solution": "class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\nclass Warrior(Character):\n    def attack(self):\n        return f'{self.name} swings sword!'\nclass Mage(Character):\n    def attack(self):\n        return f'{self.name} casts fireball!'",
  "solutionExpl": "Implements specialized attacks overriding parent Character behavior.",
  "expectedOutput": "'Alice swings sword!'",
  "extensions": [
    "Add level up attribute modifiers"
  ]
}