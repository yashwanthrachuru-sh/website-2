{
  "printNote": "SQLAlchemy Reference",
  "sections": [
    {
      "heading": "SQLAlchemy 2.0 Syntax",
      "entries": [
        {
          "syntax": "session.scalars(select(Model).where(cond))",
          "example": "s.scalars(select(User)).all()",
          "description": "Query records with select()"
        }
      ]
    }
  ]
}