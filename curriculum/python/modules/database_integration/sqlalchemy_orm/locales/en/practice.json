{
  "easy": {
    "title": "Define User ORM Model",
    "description": "Define `User(Base)` with primary key `id` and `name` string.",
    "starterCode": "# Define Base and User model",
    "expectedOutput": "True",
    "solution": "from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column\nfrom sqlalchemy import String\nclass Base(DeclarativeBase): pass\nclass User(Base):\n    __tablename__ = 'users'\n    id: Mapped[int] = mapped_column(primary_key=True)\n    name: Mapped[str] = mapped_column(String(50))",
    "hints": [
      "Use Mapped and mapped_column"
    ],
    "difficulty": "easy"
  },
  "medium": {
    "title": "ORM Insert and Query",
    "description": "Insert User('Alice') and return name using Session.",
    "starterCode": "def add_and_get(engine):\n    pass",
    "expectedOutput": "'Alice'",
    "solution": "from sqlalchemy.orm import Session\nfrom sqlalchemy import select\ndef add_and_get(engine):\n    with Session(engine) as s:\n        s.add(User(id=1, name='Alice'))\n        s.commit()\n        return s.scalars(select(User)).first().name",
    "hints": [
      "Use session.add() and session.scalars()"
    ],
    "difficulty": "medium"
  },
  "hard": {
    "title": "One-to-Many Relationship Model",
    "description": "Define User (1) to Posts (N) relationship using `relationship()`.",
    "starterCode": "# Define User and Post models",
    "expectedOutput": "True",
    "solution": "from sqlalchemy.orm import relationship\nfrom sqlalchemy import ForeignKey\nclass Post(Base):\n    __tablename__ = 'posts'\n    id: Mapped[int] = mapped_column(primary_key=True)\n    user_id: Mapped[int] = mapped_column(ForeignKey('users.id'))\n    user: Mapped['User'] = relationship(back_populates='posts')\nUser.posts = relationship('Post', back_populates='user')",
    "hints": [
      "Use ForeignKey and relationship"
    ],
    "difficulty": "hard"
  },
  "debugging": {
    "title": "Fix Detached Instance Error",
    "description": "Fix accessing lazy attribute after Session closed.",
    "buggyCode": "with Session(engine) as s: u = s.get(User, 1)\nprint(u.posts) # DetachedInstanceError!",
    "fixedCode": "with Session(engine) as s:\n    u = s.scalars(select(User).options(joinedload(User.posts))).first()\n    print(u.posts)",
    "bugs": [
      "Eager load relationship with joinedload before closing session."
    ]
  }
}