{
  "questions": [
    {
      "question": "What happens if you forget to call `conn.commit()` after executing an INSERT/UPDATE in sqlite3?",
      "answer": "The changes remain in an uncommitted transaction state inside that connection session. Once the connection closes or application terminates without commit, SQLite automatically rolls back the uncommitted changes, losing the edits.",
      "level": "intermediate"
    }
  ]
}