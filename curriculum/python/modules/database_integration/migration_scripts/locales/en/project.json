{
  "title": "Schema Version Inspector",
  "tagline": "Database migration audit tool",
  "description": "Build a database inspector querying `alembic_version` to verify environment schema sync.",
  "learningGoals": [
    "Verifying database schema versions"
  ],
  "requirements": [
    "Query alembic_version table"
  ],
  "starterCode": "def get_current_revision(conn): pass",
  "solution": "import sqlite3\ndef get_current_revision(conn):\n    try:\n        cur = conn.cursor()\n        cur.execute('SELECT version_num FROM alembic_version')\n        row = cur.fetchone()\n        return row[0] if row else None\n    except Exception:\n        return None",
  "solutionExpl": "Queries alembic_version table to retrieve active migration revision hash.",
  "expectedOutput": "'a1b2c3d4'",
  "extensions": [
    "Check against target head version"
  ]
}