{
  "examples": [
    {
      "code": "# Offline Migration Generation\n# $ alembic upgrade head --sql\n# Generates raw DDL SQL script without executing against DB!",
      "explanation": "Generates DDL SQL scripts for DBA review."
    }
  ]
}