{
  "title": "Access Permission Overlap Engine",
  "tagline": "Role-based access matrix comparator",
  "description": "Compare user permissions against required security roles using set intersection/difference.",
  "learningGoals": [
    "Set algebra for access control"
  ],
  "requirements": [
    "Find missing permissions for required role"
  ],
  "starterCode": "def check_permissions(user_perms, role_perms):\n    pass",
  "solution": "def check_permissions(user_perms, role_perms):\n    u, r = set(user_perms), set(role_perms)\n    missing = r - u\n    return {\n        'has_access': len(missing) == 0,\n        'missing': list(missing),\n        'extra': list(u - r)\n    }",
  "solutionExpl": "Uses set difference `-` to compute missing and extra user permissions.",
  "expectedOutput": "{'has_access': False, 'missing': ['write'], 'extra': []}",
  "extensions": [
    "Support multi-role aggregation"
  ]
}