{
  "examples": [
    {
      "code": "# Fast Set Operations for Large Datasets\nallowed_ip_set = {f'192.168.1.{i}' for i in range(1, 100000)}\n# Checking 1,000 incoming requests against set takes ~0.0001s (O(1) per check)",
      "explanation": "Using set lookup for IP blacklists/whitelists is orders of magnitude faster than list search."
    }
  ]
}