{
  "questions": [
    {
      "question": "Compare memory and speed of dict comprehension vs `dict(zip(keys, values))`.",
      "answer": "`dict(zip(keys, values))` is faster when merging two existing parallel sequences. Dict comprehensions are better when transforming or filtering keys/values during construction.",
      "level": "intermediate"
    }
  ]
}