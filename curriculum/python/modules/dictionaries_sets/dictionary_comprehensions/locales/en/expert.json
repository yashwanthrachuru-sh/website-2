{
  "examples": [
    {
      "code": "# Grouping with Dict Comprehension and Sets\nwords = ['apple', 'apricot', 'banana', 'bare', 'cat']\ngrouped = {c: [w for w in words if w.startswith(c)] for c in set(w[0] for w in words)}\nprint(grouped)",
      "explanation": "Nested comprehension grouping words by starting letter key."
    }
  ]
}