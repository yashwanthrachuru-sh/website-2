{
  "questions": [
    {
      "question": "What happens under the hood when a `for` loop runs in Python?",
      "answer": "Python calls `iter(obj)` to get an iterator object. In each iteration, it calls `next(iterator)`. When the sequence ends, `next()` raises a `StopIteration` exception, which the `for` loop catches silently to exit.",
      "level": "intermediate"
    }
  ]
}