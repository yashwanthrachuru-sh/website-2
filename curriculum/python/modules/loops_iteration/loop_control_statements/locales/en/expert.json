{
  "examples": [
    {
      "code": "# Breaking Outer Loop using Function Return\ndef find_matrix_target(matrix, target):\n    for r_idx, row in enumerate(matrix):\n        for c_idx, val in enumerate(row):\n            if val == target:\n                return (r_idx, c_idx)  # Cleanly exits both loops\n    return None",
      "explanation": "Returning from a function is the cleanest way to break out of multi-level nested loops."
    }
  ]
}