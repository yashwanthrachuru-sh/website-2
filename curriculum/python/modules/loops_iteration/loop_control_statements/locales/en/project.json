{
  "title": "Log File Severity Processor",
  "tagline": "Stream log parser with early filtering",
  "description": "Parse lines from a log stream, skipping debug logs (`continue`) and halting on critical errors (`break`).",
  "learningGoals": [
    "Apply break and continue to stream parsing"
  ],
  "requirements": [
    "Skip [DEBUG]",
    "Process [INFO] & [WARNING]",
    "Halt on [FATAL]"
  ],
  "starterCode": "def process_logs(log_lines):\n    pass",
  "solution": "def process_logs(log_lines):\n    processed = []\n    for line in log_lines:\n        if '[DEBUG]' in line:\n            continue\n        if '[FATAL]' in line:\n            processed.append('CRITICAL_HALT')\n            break\n        processed.append(line)\n    return processed",
  "solutionExpl": "Filters logs using continue and halts parsing on FATAL log line.",
  "expectedOutput": "['[INFO] Started', 'CRITICAL_HALT']",
  "extensions": [
    "Count skipped debug lines"
  ]
}