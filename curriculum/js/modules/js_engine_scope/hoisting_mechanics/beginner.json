{
  "motivation": "How variables and functions move to top of scope.",
  "whyExists": "Functions eliminate code duplication and enable **abstraction** — hiding complexity behind a simple interface. A function called `calculate_tax(price)` doesn't expose HOW tax is calculated; callers only need to know WHAT it does. This separation allows the implementation to change without affecting callers. Large systems like operating systems, web frameworks, and databases are composed of thousands of functions that each do one thing well.",
  "realWorldAnalogy": "Imagine you are working in a warehouse. Instead of tossing boxes around randomly, you place them into labeled shelves. When a package arrives, you look up its shelf label to update the stock index. Similarly, **Hoisting Mechanics** acts as a named workspace or rule that coordinates statements, keeping variables safe and ensuring operations execute only when matching conditions are met.",
  "simpleExplanation": "To learn Hoisting Mechanics from first principles, start by looking at how data resolves at runtime. Imagine your processor executing instructions sequentially. Hoisting Mechanics defines how values bind, operations evaluate, and stack structures are clean.",
  "visualDiagram": "```text\n[User Input] ──► [Process Scope: Hoisting Mechanics] ──► [Memory Address] ──► [Console Output]\n```",
  "syntaxExplanation": "Verify that all brackets, colons, keywords, and assignments match the javascript syntax specifications. Avoid naming collisions by using scoped namespaces.",
  "stepByStepExecution": [
    {
      "step": 1,
      "desc": "Allocate memory segment and load bindings."
    },
    {
      "step": 2,
      "desc": "Process instruction registers."
    },
    {
      "step": 3,
      "desc": "Pop current stack frame."
    }
  ],
  "memoryDiagram": {
    "type": "variables",
    "theme": "dark",
    "slots": [
      {
        "address": "0x7fff01",
        "name": "hoisting_mechanics_ref",
        "value": "0x7fff08"
      },
      {
        "address": "0x7fff08",
        "name": "hoisting_mechanics_val",
        "value": "100"
      }
    ]
  },
  "examples": [
    {
      "title": "Basic Syntax Pattern",
      "code": "console.log(\"Running Hoisting Mechanics...\");",
      "explanation": "This output statement verifies execution."
    }
  ]
}